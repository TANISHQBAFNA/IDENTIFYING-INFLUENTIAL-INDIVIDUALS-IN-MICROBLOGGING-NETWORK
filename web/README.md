# Influence dashboard

Vite + React app for the follow-graph explorer.

```bash
npm install
npm run dev     # local page
npm test        # unit tests
npm run build   # GitHub Pages-ready static output in dist/
```

Sample data is served from `public/sample_network.csv`.
